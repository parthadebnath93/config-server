eureka:
   client:
      serviceUrl:
         defaultZone: http://localhost:8761/eureka
      instance:
      preferIpAddress: true
      fetchRegistry: true
      registerWithEureka: true
---
instance:
    hostname: localhost
